name: Release

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Analyse the commits and print what would be released, without tagging or publishing"
        type: boolean
        default: true

concurrency:
  group: release
  cancel-in-progress: false

permissions:
  contents: write        # create the tag, the GitHub Release, and commit the regenerated CHANGELOG
  issues: write          # semantic-release comments on released issues
  pull-requests: write   # ... and on released pull requests
  id-token: write        # request the OIDC token for NuGet Trusted Publishing

env:
  DOTNET_NOLOGO: true
  DOTNET_CLI_TELEMETRY_OPTOUT: true

jobs:
  release:
    name: semantic-release
    runs-on: ubuntu-latest
    # A release must never publish something CI has not built and tested on this exact commit.
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
          persist-credentials: true
          # The default GITHUB_TOKEN cannot push to main: the branch ruleset requires a pull
          # request and only bypasses for the repo-owner role, which the Actions bot token does
          # not hold. RELEASE_TOKEN is a fine-grained PAT scoped to this repo (Contents: R/W)
          # from an account that does hold that role, so @semantic-release/git's push and
          # @semantic-release/github's release creation (below) both go through as that identity.
          token: ${{ secrets.RELEASE_TOKEN }}

      - name: Setup .NET
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: 10.0.x

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm

      - name: Install release tooling
        run: npm ci

      - name: Restore
        run: dotnet restore CanKit.Pro.sln

      - name: Build
        run: dotnet build CanKit.Pro.sln --configuration Release --no-restore

      # The gate: nothing is tagged or pushed to nuget.org unless the tests pass on this commit.
      - name: Test
        run: dotnet test CanKit.Pro.sln --configuration Release --no-build

      # NuGet Trusted Publishing: exchange this run's OIDC token for a short-lived (1h) API key.
      # Requested right before the release step so it doesn't expire before the push happens.
      - name: NuGet login (OIDC -> temp API key)
        uses: NuGet/login@v1
        id: nuget_login
        with:
          user: ${{ secrets.NUGET_USER }}

      # semantic-release decides the version from the conventional commits since the last tag,
      # then (via @semantic-release/exec) packs with exactly that version and pushes to nuget.org,
      # writes CHANGELOG.md back to main, tags vX.Y.Z and opens the GitHub Release.
      - name: Release
        run: npx semantic-release ${{ (github.event_name == 'workflow_dispatch' && inputs.dry_run) && '--dry-run' || '' }}
        env:
          GITHUB_TOKEN: ${{ secrets.RELEASE_TOKEN }}
          NUGET_API_KEY: ${{ steps.nuget_login.outputs.NUGET_API_KEY }}
